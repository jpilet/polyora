ADD_EXECUTABLE(buildtree buildtree.cpp)

ADD_DEPENDENCIES(buildtree polyora)

INCLUDE_DIRECTORIES( ${OpenCV_INCLUDE_DIRS} )
TARGET_LINK_LIBRARIES(buildtree polyora ${OpenCV_LIBS} )

INSTALL(TARGETS buildtree RUNTIME DESTINATION bin COMPONENT tools)
